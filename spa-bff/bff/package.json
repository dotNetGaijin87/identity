{
  "name": "spa-bff-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Backend-for-Frontend: runs OIDC auth-code+PKCE against the IdP, holds tokens server-side, sets an httpOnly session cookie, and records a per-session trace for the SPA visualizer.",
  "scripts": {
    "start": "node server.mjs"
  }
}
