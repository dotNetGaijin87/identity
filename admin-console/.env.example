# Base URL the API client targets. MSW intercepts requests to this origin/path.
VITE_API_URL=/api
# Toggle the MSW mock backend in dev/preview. This demo has no real backend, so keep it on.
VITE_API_MOCKING=enabled
